#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@parcel/watcher/node_modules/detect-libc/bin/detect-libc.js" "$@"
  ret=$?
else 
  node  "$basedir/../@parcel/watcher/node_modules/detect-libc/bin/detect-libc.js" "$@"
  ret=$?
fi
exit $ret
